import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lkpd.nitakarawo',
  appName: 'Petualangan Nita Karawo',
  webDir: 'dist'
};

export default config;
